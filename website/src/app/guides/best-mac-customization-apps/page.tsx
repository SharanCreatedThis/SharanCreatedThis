import { GuidePage, guideMetadata } from "@/lib/guides";

export const metadata = guideMetadata("best-mac-customization-apps");
export default function Page() {
  return <GuidePage slug="best-mac-customization-apps" />;
}
