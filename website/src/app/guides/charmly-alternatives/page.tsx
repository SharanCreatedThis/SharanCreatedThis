import { GuidePage, guideMetadata } from "@/lib/guides";

export const metadata = guideMetadata("charmly-alternatives");
export default function Page() {
  return <GuidePage slug="charmly-alternatives" />;
}
