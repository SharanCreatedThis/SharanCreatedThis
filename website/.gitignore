.vercel

# Local wrangler/miniflare state from `wrangler pages dev`.
.wrangler/
/src/data/charms.generated.ts

# Local overrides only. .env.production is committed on purpose — see it.
.env.local
.env*.local
